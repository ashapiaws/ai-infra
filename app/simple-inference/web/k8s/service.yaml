apiVersion: v1
kind: Service
metadata:
  name: chat-ui
  namespace: inference-app
  labels:
    app: chat-ui
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app: chat-ui
