# Accepted Patterns

Production-validated deployment patterns and configurations.

## Structure

- `inference/` — vLLM serving patterns on EKS
- `training/` — Ray training patterns (future)
- `gateway/` — AI gateway routing configurations
- `observability/` — Monitoring and alerting patterns
